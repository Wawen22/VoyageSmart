{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --omit=optional --no-optional",
        "buildCommand": "npm run build"
      }
    }
  ]
}
